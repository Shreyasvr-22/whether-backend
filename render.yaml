services:
  - type: web
    name: ai-back
    env: python
    plan: free
    pythonVersion: 3.11.7
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn -w 2 -k uvicorn.workers.UvicornWorker -b 0.0.0.0:8000 --timeout 120 main:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.7
